<UserControl x:Class="AutoRentalApp.Views.CarsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Панель поиска и действий -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,15">
            <TextBox x:Name="SearchBox" Width="300" FontSize="14" 
                     Padding="8" BorderBrush="#BDBDBD" BorderThickness="1"
                     TextChanged="SearchBox_TextChanged" 
                     VerticalContentAlignment="Center"/>
            <Button Content="Поиск" Style="{StaticResource PrimaryButton}" 
                    Click="SearchButton_Click" Margin="10,0,0,0" Width="100"/>
            <Button Content="Добавить автомобиль" Style="{StaticResource PrimaryButton}" 
                    Click="AddCarButton_Click" Margin="10,0,0,0" Width="186" Background="#2196F3"/>
            <Button Content="Обновить" Style="{StaticResource PrimaryButton}" 
                    Click="RefreshButton_Click" Margin="10,0,0,0" Width="120"/>
        </StackPanel>

        <!-- Таблица автомобилей -->
        <DataGrid Grid.Row="1" x:Name="CarsDataGrid" 
                  AutoGenerateColumns="False" IsReadOnly="True"
                  SelectionMode="Single" SelectionChanged="CarsDataGrid_SelectionChanged"
                  RowDetailsVisibilityMode="VisibleWhenSelected" BorderBrush="#E0E0E0" BorderThickness="1">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding CarID}" Width="60"/>
                <DataGridTextColumn Header="Гос. номер" Binding="{Binding PlateNumber}" Width="120"/>
                <DataGridTextColumn Header="Марка" Binding="{Binding Brand}" Width="100"/>
                <DataGridTextColumn Header="Модель" Binding="{Binding Model}" Width="100"/>
                <DataGridTextColumn Header="Год" Binding="{Binding Year}" Width="70"/>
                <DataGridTextColumn Header="Цвет" Binding="{Binding Color}" Width="90"/>
                <DataGridTextColumn Header="Класс" Binding="{Binding CarClass}" Width="90"/>
                <DataGridTextColumn Header="Цена/сутки" Binding="{Binding DailyPrice}" Width="100"/>
                <DataGridTextColumn Header="Пробег" Binding="{Binding Mileage}" Width="90"/>
                <DataGridTextColumn Header="Статус" Binding="{Binding CarStatus.StatusName}" Width="120"/>
            </DataGrid.Columns>
            <DataGrid.RowDetailsTemplate>
                <DataTemplate>
                    <Border Background="#F5F7FA" BorderBrush="#E0E0E0" BorderThickness="1" Margin="0,5,0,5">
                        <StackPanel Margin="10">
                            <TextBlock Text="Действия с автомобилем:" FontWeight="Bold" Margin="0,0,0,8"/>
                            <StackPanel Orientation="Horizontal">
                                <Button Content="✏ Редактировать" Click="EditButton_Click" 
                                        Tag="{Binding}" Style="{StaticResource PrimaryButton}" 
                                        Width="130" Margin="0,0,10,0" Background="#2196F3"/>
                                <Button Content="🗑 Удалить" Click="DeleteButton_Click" 
                                        Tag="{Binding}" Style="{StaticResource PrimaryButton}" 
                                        Width="130" Background="#FF5252"/>
                            </StackPanel>
                        </StackPanel>
                    </Border>
                </DataTemplate>
            </DataGrid.RowDetailsTemplate>
        </DataGrid>

        <!-- Статусная строка -->
        <TextBlock Grid.Row="2" x:Name="StatusText" Text="Загрузка данных..." 
                   Foreground="#757575" HorizontalAlignment="Left" Margin="0,10,0,0" FontSize="13"/>
    </Grid>
</UserControl>